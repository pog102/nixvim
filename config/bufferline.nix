{
  plugins = {
    bufferline = {
      enable = true;
      # diagnostics = true;
      # separatorStyle = "padded_slant";
      settings = {
        options = {
          hover.enabled = true;
          highlights.indicator_selected.underline = true;
          always_show_bufferline = false;
        };
      };
    };
  };
}
